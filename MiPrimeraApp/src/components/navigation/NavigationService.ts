import { createNavigationContainerRef } from "@react-navigation/native"
import { RootStackParmList } from "./StackNavigator";

export const navigationRef = createNavigationContainerRef<RootStackParmList>();